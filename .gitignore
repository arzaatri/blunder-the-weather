# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Project notes (personal, not for version control)
NOTES.txt
TODO.txt

# Local runtime state
data/
logs/
.pids/
.dagster_home/*
!.dagster_home/dagster.yaml
.env

# Trained model artifacts (local mirror of the MinIO models/ prefix -- binaries,
# not source; anchored to repo root so it doesn't shadow src/blunder_the_weather/models/)
/models/

