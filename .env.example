# Copy to .env and fill in local values. .env is gitignored.

# MinIO root credentials. These double as the S3 access key ID / secret access key
# that our Python code and Dagster's S3Resource use to talk to MinIO.
MINIO_ROOT_USER=blunder_admin
MINIO_ROOT_PASSWORD=change_me_local_dev_only
