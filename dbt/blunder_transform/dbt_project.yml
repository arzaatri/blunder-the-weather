name: "blunder_transform"
version: "1.0.0"
config-version: 2

profile: "blunder_transform"

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  blunder_transform:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: external
